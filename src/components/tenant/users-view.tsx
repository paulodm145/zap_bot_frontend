'use client';
import { useMemo,useState } from 'react';import { Plus,Search,Trash2,UserRound } from 'lucide-react';import { AppShell } from '@/components/layout/app-shell';import { Badge } from '@/components/ui/badge';import { Button } from '@/components/ui/button';import { DataTable,type DataTableColumn } from '@/components/ui/data-table';import { CrudModal } from './crud-modal';import { useCreateUser,useDeleteUser,useUsers,useUserStatus } from '@/hooks/tenant/use-users';import { useDebouncedValue } from '@/hooks/common/use-debounced-value';import type { Role,TenantUser } from '@/features/tenant/types';import { isApiError } from '@/lib/api/api-error';import styles from './tenant.module.css';
const roleLabel:Record<Role,string>={ADMIN_TENANT:'Administrador',GESTOR:'Gestor',ATENDENTE:'Atendente'};
export function UsersView(){const[search,setSearch]=useState(''),[skip,setSkip]=useState(0),[take,setTake]=useState(20),[open,setOpen]=useState(false),[nome,setNome]=useState(''),[email,setEmail]=useState(''),[senha,setSenha]=useState(''),[papel,setPapel]=useState<Role>('ATENDENTE');const list=useUsers(useDebouncedValue(search),skip,take),create=useCreateUser(),status=useUserStatus(),remove=useDeleteUser();const columns=useMemo<DataTableColumn<TenantUser>[]>(()=>[{id:'name',header:'Usuário',width:'32%',render:u=><div className={styles.entity}><span><UserRound size={16}/></span><div><strong>{u.nome}</strong><small>{u.email}</small></div></div>},{id:'role',header:'Papel',width:'22%',render:u=>roleLabel[u.papel]},{id:'sectors',header:'Setores',width:'25%',hideOnMobile:true,render:u=>u.setores?.map(s=>s.nome).join(', ')||'Sem setor'},{id:'status',header:'Status',width:'12%',render:u=><Badge tone={u.ativo?'success':'neutral'}>{u.ativo?'Ativo':'Inativo'}</Badge>}],[]);const error=list.error??status.error??remove.error;return <AppShell title="Usuários" subtitle="Acessos, papéis e situação das contas." actions={<Button icon={<Plus size={17}/>} onClick={()=>setOpen(true)}>Novo usuário</Button>}>{error&&<div className={styles.error}>{isApiError(error)?error.message:'Não foi possível concluir.'}</div>}<DataTable columns={columns} data={list.data?.dados??[]} getRowId={u=>u.public_id} pagination={{skip:list.data?.skip??skip,take:list.data?.take??take,total:list.data?.total??0}} onPaginationChange={p=>{setSkip(p.skip);setTake(p.take)}} loading={list.isLoading||list.isFetching} emptyTitle="Nenhum usuário encontrado" emptyDescription="Cadastre a primeira pessoa ou altere a busca." toolbar={<label className={styles.search}><Search size={15}/><input value={search} onChange={e=>{setSearch(e.target.value);setSkip(0)}} placeholder="Buscar usuário..."/></label>} rowActions={u=><><Button variant="ghost" onClick={()=>status.mutate({id:u.public_id,ativo:!u.ativo})}>{u.ativo?'Desativar':'Ativar'}</Button><Button variant="ghost" size="icon" icon={<Trash2 size={16}/>} aria-label={`Excluir ${u.nome}`} onClick={()=>{if(confirm(`Excluir ${u.nome}?`))remove.mutate(u.public_id)}}/></>}/>{open&&<CrudModal title="Novo usuário" subtitle="Crie o acesso e defina o nível de permissão inicial." submitLabel="Cadastrar usuário" pending={create.isPending} error={create.error?(isApiError(create.error)?create.error.message:'Falha ao cadastrar.'):null} onClose={()=>setOpen(false)} onSubmit={e=>{e.preventDefault();create.mutate({nome,email,senha,papel},{onSuccess:()=>setOpen(false)})}}><div className={styles.form}><label className={styles.wide}><span>Nome completo</span><input value={nome} onChange={e=>setNome(e.target.value)} required placeholder="Nome da pessoa"/></label><label><span>E-mail</span><input type="email" value={email} onChange={e=>setEmail(e.target.value)} required placeholder="pessoa@empresa.com"/></label><label><span>Papel</span><select value={papel} onChange={e=>setPapel(e.target.value as Role)}><option value="ATENDENTE">Atendente</option><option value="GESTOR">Gestor</option><option value="ADMIN_TENANT">Administrador</option></select></label><label className={styles.wide}><span>Senha inicial</span><input type="password" value={senha} onChange={e=>setSenha(e.target.value)} minLength={12} required autoComplete="new-password"/><small>12 caracteres ou mais, com maiúscula, minúscula e número.</small></label></div></CrudModal>}</AppShell>}
